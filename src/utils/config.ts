import { Config } from 'src/types';

export const CONFIG: Config = {
  1: {
    numEnemies: 10,
    minWordLength: 3,
    maxWordLength: 5,
    minSpeed: 1,
    maxSpeed: 2,
    maxSpawnDelay: 5000,
  },
};
